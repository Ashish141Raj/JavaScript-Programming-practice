// Take two numbers and divide them using / operator.

const prompt = require("prompt-sync")();

let num1 = prompt("Enter value of num1: ");
let num2 = prompt("Enter value of num2: ");

let result = num1/num2;
console.log(num1, "/", num2, "=", result);
